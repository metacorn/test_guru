<div class="mt-2 row justify-content-center">
  <div class="col col-md-3">
    <% flash.each do |key, value| %>
      <%= content_tag :div, value.html_safe, class: "#{flash_class(key)} text-center" %>
    <% end %>
  </div>
</div>